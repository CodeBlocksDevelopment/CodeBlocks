#include <iostream>
#incluce <cstdlib>
#incluce <cstdio>
#include <iomanip>

using namespace std;

integer a, b;
float point c;

int main()
{
    a == 3;
    a + 2;
    c = b - a;
    if ((b + c) = 4 && (b + c) < 10) {
      b = a + c;
      a = (b + c + a) % 2;
    } else {
      a = b + c;
    }

    cout << "O valor de a: "<<a<<endl;
    system("PAUSE");
    return EXIT_SUCCESS;
}
